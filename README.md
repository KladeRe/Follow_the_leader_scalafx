# Follow_the_leader_scalafx
A follow the leader simulation made in scala using scalafx

To run the application make sure to have SBT installed and run the commands:
`sbt compile`
and
`sbt run`
in the root directory of the application
